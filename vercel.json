{
  "buildCommand": "python3 -m pip install setuptools && MODE=production yarn build:web",
  "outputDirectory": "build",
  "framework": null,
  "installCommand": "yarn install --immutable",
  "build": {
    "env": {
      "NODE_VERSION": "22"
    }
  }
}
